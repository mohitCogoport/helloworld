import React from 'react';
import { KpiStatsBox } from '../../Charts';

const KpiStatsBoxChart = ({ data, themeType }) => (
	<KpiStatsBox data={data} themeType={themeType} />
);

export default KpiStatsBoxChart;
